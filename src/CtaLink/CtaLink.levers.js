/**
 * Display type for the CTA link; shared by CTA link buttons
 */
export const ctaLinkDisplay = ['inline-block', 'block'];

/**
 * CTA link icon options; shared by CTA link buttons
 */
export const ctaLinkIcons = ['none', 'external', 'lock', 'download', 'video', 'jump', 'email'];

/**
 * Colors for the CTA link
 */
export const ctaLinkColors = ['red', 'blue', 'white'];
