/**
 * Grid gap or not
 */
export const gridGap = [true, false];

/**
 * Available number of grid columns at XS breakpoint (1 to 12)
 */
export const gridColsXs = Array.from({length: 12}, (_, i) => i + 1);

/**
 * Available number of grid columns at SM breakpoint (1 to 12)
 */
export const gridColsSm = Array.from({length: 12}, (_, i) => i + 1);

/**
 * Available number of grid columns at MD breakpoint (1 to 12)
 */
export const gridColsMd = Array.from({length: 12}, (_, i) => i + 1);

/**
 * Available number of grid columns at LG breakpoint (1 to 12)
 */
export const gridColsLg = Array.from({length: 12}, (_, i) => i + 1);

/**
 * Available number of grid columns at XL breakpoint (1 to 12)
 */
export const gridColsXl = Array.from({length: 12}, (_, i) => i + 1);

/**
 * Available number of grid columns at 2XL breakpoint (1 to 12)
 */
export const gridCols2Xl = Array.from({length: 12}, (_, i) => i + 1);
